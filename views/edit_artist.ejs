<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-r from-blue-400 to-purple-500 min-h-screen flex items-center justify-center flex-col p-4">
    <h1 class="text-white text-4xl font-bold mb-6">Edit Artist</h1>
    <h2 class="text-white text-2xl mb-4">Artist: <%= artist.artists %></h2>
    <!-- Edit artist form -->
    <form action="/edit/<%= artist.id %>" method="POST" id="edit-form" class="bg-white rounded-lg shadow-md p-6 w-full max-w-md">
        <div class="mb-4">
            <label for="artist" class="block text-gray-700 font-semibold mb-2">Artist Name</label>
            <input type="text" id="artist" name="artist" value="<%= artist.artists %>" required class="border border-gray-300 rounded-lg w-full p-2 focus:outline-none focus:ring focus:ring-blue-300" />
        </div>
        <div class="mb-4">
            <label for="likes" class="block text-gray-700 font-semibold mb-2">Amount of Likes</label>
            <input type="number" id="likes" name="likes" value="<%= artist.likes %>" required class="border border-gray-300 rounded-lg w-full p-2 focus:outline-none focus:ring focus:ring-blue-300" />
        </div>
        <button type="submit" name="add" class="bg-blue-600 text-white font-semibold py-2 px-4 rounded shadow hover:bg-blue-700 transition">Update Artist</button>
    </form>
</body>
</html>
